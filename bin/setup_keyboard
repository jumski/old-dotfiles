# set keyboard repeat
xset r rate 250 100

options="-option ctrl:menu_rctrl -option altwin:ctrl_win -option ctrl:nocaps"

if [[ "swapcaps" == $1 ]]; then
  options="$options -option ctrl:swapcaps"
fi

setxkbmap -option $options
